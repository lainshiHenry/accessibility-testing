declare class EventForwarder {
    private emitter;
    private tasks;
    on: Cypress.PluginEvents;
    constructor();
    /**
     * Forwards all the events registered so far to cypress
     */
    forward(on: Cypress.PluginEvents): void;
}
export default EventForwarder;
